import React from "react";
import { createRoot } from "react-dom/client";
import App from "./App"; // Importamos el componente

const root = createRoot(document.getElementById("root"));
root.render(<App />);
